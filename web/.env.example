# Opt-in only. Leave blank until provider/model and data handling are approved.
ANALYSIS_PROVIDER=
ANALYSIS_MODEL=
OPENAI_API_KEY=
# Operator attestation: confirm selected project AND model are covered by
# provider-approved content non-retention controls. store:false alone is NOT enough.
ANALYSIS_RETENTION_CONFIRMED=false
# Trusted absolute HTTPS origin supplied by deployment. Never use forwarded headers.
SITE_ORIGIN=
