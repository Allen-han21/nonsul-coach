---
last_updated: 2026-09-14
branch: main
base_commit: 05eedf8
approval: approved
current_step: 7
status: awaiting_required_input
next_action: "AI 제공자·모델·비저장 조건을 확인해 실제 분석을 검증하고, Sites 배포용 Git 예외 승인 후 8단계 진행"
document_commit_policy: local_commits_only_no_push
---

# nonsul-coach 프로토타입 구현 Handoff

제품 범위는 [README](../README.md), [명세](prototype.md), 최신 사용자 요청을 따른다. 실제 작업 공간은 `/Users/allen/Dev/Repo/nonsul-coach`의 `main`이다. 대화가 열린 `sprint-1` 작업 트리와 다르며 그 작업 트리는 변경하지 않았다.

## 승인과 제약

- 사용자가 1~8단계 구현, 의존성 설치, 로컬 검증, Sites 제한 공유 배포를 승인했다.
- 사용자가 현재 기준 소스를 자연스러운 단위로 나눈 로컬 커밋으로 남기도록 허용했다. 푸시·PR 생성은 계속 금지되어 있다. 기준 커밋 `05eedf8`은 변하지 않았으며 이후 작업은 main의 로컬 커밋으로 보존한다.
- 대학과 가이드는 사용자 제공 자료만 사용한다. 성신여자대학교 인문계열만 지원한다.
- 2026학년도 인문 1교시 2문항과 추가 요청한 2027학년도 인문 모의 2문항, 총 4문항이다.
- 학생용 한 화면. 학생 정보·답안·AI 요청/응답·분석 이력을 저장하거나 로그로 남기지 않는다.
- 공식 근거 없는 총점·등급·합격 가능성, 완성 답안, 예시답안 문장 제공을 하지 않는다.
- 단계의 완료 기준을 확인한 뒤 다음 단계로 진행한다. 실제 모델 정확도·교사 검토·배포 성공을 합성 테스트로 대체하여 완료라고 보고하지 않는다.

## 단계 진행표

| 단계 | 상태 | 완료 근거 / 남은 작업 |
| --- | --- | --- |
| 1. 평가 자료 확정 | done | [평가 패키지](evaluation-packages.md), [출처 데이터](evaluation/source.json). 원본 해시 및 네 문항의 텍스트·페이지 이미지 대조 완료. |
| 2. 프로젝트 기반 | done | `web/` Sites+shadcn, 한글 테마·메타데이터, 초기 개발 실행·빌드 통과. |
| 3. 입력 화면 | done | 네 문항 선택 시 공식 유의사항·문제·전체 제시문 자동 표시, 답안 입력란·개인정보 안내·참고 글자 수. 실제 미리보기와 브라우저 검증 완료. |
| 4. 평가 데이터·규칙 | done | 엄격한 스키마, 출처 연결, 보수적인 규칙 검사. 경계·예외 테스트 통과. |
| 5. AI 분석·재검증 | local_pass | 후보→독립 재검토→정확한 원문 인용·정식 출처 재검증. 합성 제공자로 완료 기준 확인. 실제 모델 연결 미완료. |
| 6. 피드백·재분석 화면 | local_pass | 한 화면에 기준별 진단·우선 과제 최대 3개·근거 선택·출처·수정 질문/행동·재분석. 데스크톱/모바일 합성 왕복 통과. |
| 7. 품질·안전성 | local_pass_live_pending | [검증 기록](prototype-verification.md). 테스트 22개, 화면 테스트 8개, 타입·린트·빌드·클라이언트 노출 검사 통과. 실제 AI와 교육적 유효성은 미검증. |
| 8. 배포·최종 확인 | not_started | AI 연결과 비저장 조건 확인, Git 금지 예외 결정 필요. 아직 Sites 생성·업로드·배포 없음. |

## 이번 재개 작업의 구현

- `web/lib/server/analysis.ts`: 제공자 인터페이스, 2단계 구조화 분석. 후보/리뷰 스키마 검증, 가짜·중복·누락 ID와 허위 인용 처리, 반복 인용 위치 해석, 불확실한 지적을 우선 과제에서 제외.
- 모델이 자유 작성한 이유·행동·완성 문장은 응답에 포함하지 않는다. 검토된 기준별 문구 + 판단 종류 + 원문 근거 위치를 조합한다. 실제 의미 판단의 정확성까지 보장하는 방식은 아니다.
- `openai-provider.ts`: 선택 가능한 OpenAI Responses 어댑터를 준비했지만 기본 비활성. 제공자·모델·키·비저장 확인값이 모두 있어야 사용 가능. 키/본문 로그·대화 ID·파일 업로드·자동 재시도 없음.
- `http.ts`, `app/api/analyze/route.ts`: 서버 전용 진입점, 동일 출처 JSON 요청, 엄격한 입력·바이트 제한, 기본 식별정보 패턴 차단, 분석 시간 제한, no-store 응답, 안전한 오류 코드.
- `app/page.tsx`, `components/feedback.tsx`: 로딩/취소/오류/재시도, 수정 전 결과 안내, 인용 선택, 문항 변경 시 결과 해제, 재분석 시 이전 답안 미전송.
- `app/layout.tsx`: 기존 생성 공유 이미지 `public/og.png` 연결. 현재 기본 절대 URL은 로컬용이며 배포 시 실제 `SITE_ORIGIN`으로 재검증해야 한다.
- 사용하지 않는 생성 템플릿 57개와 해당 의존성을 정리했다. 복구용 백업은 `/tmp/nonsul-starter-backup.0rUGLH/`. 공식 자료나 사용자 작성 파일은 삭제하지 않았다.

## 검증과 실행 상태

- 실행 위치: `/Users/allen/Dev/Repo/nonsul-coach/web`.
- `npm test`: 22개 통과.
- `npm run lint`, `npm run typecheck`, `npm run build`: 통과.
- `npm run test:browser`: 8개 통과. 동일 8개를 빌드 산출물의 로컬 Worker 환경에서도 통과했다.
- `node scripts/verify-build.mjs`: 공개 자산 16개, 화면용 공식 문제 자료와 검토 원본 일치, 공식 예시답안/키 설정의 브라우저 노출 없음.
- 의존성 취약점 검사 0건. `git diff --check` 통과.
- 현재 로컬 미리보기 `http://localhost:3000/` 유지. 개발 서버의 기존 exec 세션은 59842. 끊겼다면 정확한 URL 상태만 먼저 확인한 뒤 필요할 때 재실행한다.
- 빌드 검증용 Worker 세션 51839 (`http://localhost:4173/`)는 검증 완료 후 종료했다.
- 표준 Browser는 연결 가능한 브라우저가 없었고 Superset browser CLI는 로그인이 없었다. 로그인·쿠키를 변경하지 않았다. 별도 Playwright Chromium으로 합성 자료만 검증했다. 이미 알려진 실패에 대해 탐색/설치를 반복하지 않는다.
- 브라우저 테스트는 운영 가짜 모드를 사용하지 않는다. 성공 흐름은 테스트의 요청 대체 기능과 주입한 합성 제공자를 사용한다. 실제 미연결 API가 503을 반환하고 입력을 보존하는 경로도 검증했다.
- 공식 PDF와 예시답안·해설·채점 내부 자료는 클라이언트에 배포하지 않는다. 화면에는 `web/lib/question-content.json`의 공식 유의사항·문제·제시문만 포함하고, `web/lib/server/official.json` 전체는 서버 번들에만 존재한다.
- 그림 수치는 임의 복원하지 않는다. 분석은 해당 논술 제시문 범위이며 현재 연금 제도·법률 안내가 아니다.
- 사용자 수정 1에 따라 문제·제시문 수동 입력을 제거했다. 선택한 실제 기출 또는 모의논술의 공식 원문을 읽기 전용으로 표시하고, 2026 실제 기출의 인구상황판은 PDF에서 추출한 원본 그림을 사용한다. 분석 API는 학생 답안만 받으며 공식 자료는 서버 평가 패키지에서 정한다.

## 사용자에게 필요한 결정

1. 사용할 AI 제공자·모델 또는 기존 분석 서버와 비저장 조건. 비동기 질문을 보냈으나 아직 답변 없음. 실제 외부 호출 0회, 키 생성/설정 없음.
2. OpenAI 사용 시 `store:false`만으로 외부 비보관이 보장되지 않는다. 승인된 비보관 제어가 선택 프로젝트·모델에 적용되는지 확인해야 한다. 확인 플래그는 운영자의 진술일 뿐 외부 설정을 변경하지 않는다. 자세한 공식 문서와 조건은 [검증 기록](prototype-verification.md) 참조.
3. OpenAI 키 관리 스킬은 현재 없어 사용하지 않았다. 선택될 경우 OpenAI Developers 플러그인 활성화 또는 안전한 기존 서버 설정 흐름이 필요하다. 키를 채팅에 요구하지 않는다.
4. Sites는 정확한 배포 소스의 푸시와 그 SHA를 필수로 요구한다. 로컬 커밋은 허용되었지만 푸시는 계속 금지되어 있으므로, 별도 승인 전에는 Sites 생성·소스 업로드·배포, 가짜 SHA, 별도 저장소 임의 푸시를 하지 않는다. 승인을 요청할 때 배포 소스만의 범위와 원격을 명확히 한다.

## 재개 순서

1. 위 결정/자격 증명이 도착했는지 확인한다. 이미 완료된 스캐폴딩·이미지 생성·출처 추출은 반복하지 않는다.
2. 실제 제공자와 비저장 조건을 확인하고 합성 답안으로 네 문항 및 오류·수정 시나리오를 실제 모델로 검증한다. 필요 시 5~7단계 구현을 보정하고 전체 테스트를 다시 실행한다.
3. 교사가 실제 참여했다는 근거는 아직 없다. 검토 가능한 화면 구조와 실제 교육적 유효성은 구분한다. 검토 체크리스트는 검증 기록에 있다.
4. 7단계의 남은 검증을 확인하고, Git 예외 승인 후에만 Sites Hosting 절차 진행. `web/.openai/hosting.json`에는 아직 `project_id`가 없다. 최초 생성은 한 번만 하고 반환 ID를 즉시 보존한다.
5. 실제 원점 메타데이터, 호스팅 비저장·접근 제한, 안전한 런타임 설정을 확인한 소스로 빌드·정확한 소스 푸시·버전 저장·소유자 제한 배포·실제 왕복 검증을 수행한다.
6. 검증된 배포 URL이 있을 때만 전체 목표를 완료로 보고한다.

## 출처 재생성

`scripts/prepare-evaluation.mjs`에 사용자 제공 PDF 경로를 전달한다. 원본 SHA-256이 다르면 중지한다. 검토 원본, 서버 전용 평가 자료, 내부 해설·예시를 제외한 화면용 문제 자료를 함께 생성하므로 기존 검토와 원본 해시를 먼저 확인한다.

## 세션 기록

### 2026-09-14 — 수정 1: 공식 문제 자료 자동 표시

- 기출 연도·문항 선택에 따라 공식 유의사항·선택 문제·동일 시험의 전체 제시문을 읽기 전용으로 표시한다.
- 문제·제시문 수동 입력과 분석 요청 필드를 제거하고, 서버가 선택 패키지의 공식 자료만 분석에 사용하도록 경계를 단순화했다.
- 실제 기출의 인구상황판 원본 그림을 포함했으며 네 문항 전환, 모바일·데스크톱, 개발 서버·빌드 Worker, 원문·내부 자료 노출 경계를 다시 검증했다.

### 2026-09-14 — 토큰 한도 후 이어서 구현

- 이전 입력·규칙 검사 지점에서 재개했다.
- 2단계 AI 검증 엔진, 선택적 OpenAI 어댑터, API, 결과·재분석 화면과 안전성 검사를 구현했다.
- 로컬 개발 및 빌드 Worker에서 검증 완료. 외부 AI 연결·Sites 배포는 필수 결정 대기.
- 중단/재개 메시지를 새로운 스캐폴딩 요청으로 해석하지 않았으며, 이후 사용자 요청에 따라 변경을 자연스러운 단위의 로컬 커밋으로 보존했다.

### 2026-09-14 — 초기 기록

- main 브랜치와 기준 커밋 `05eedf8`을 확정하고 명세·승인·진행 기록을 분리했다.
- 사용자 제공 가이드와 추가 모의논술 요청을 반영해 1단계를 완료하고 Sites 기반·입력 화면·규칙 검사까지 진행했다.
